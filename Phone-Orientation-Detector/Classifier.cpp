/**
 * @file Classifier.cpp
 * @brief Implements the abstract base classifier file.
 * @author Everyone
 * @date April 2026
 */

 // Abstract base class - no implementation needed
 // All methods are pure virtual, destructor is defined in header as = default

#include "Classifier.h"

// No implementation needed - all methods are pure virtual
// Destructor is already defined in the header as virtual ~Classifier() = default;